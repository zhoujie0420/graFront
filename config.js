

const remote = "123.56.194.88"
const local = "localhost"
console.log(remote);
console.log(local);
export const localUrl = `${local}`
export const apiUrl = `http://${local}:8081`
export const wsUrl = `${local}`